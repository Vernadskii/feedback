name: Backend CI

on:
  pull_request:
    branches:
      - develop  # default branch

jobs:
  check_changes:
    runs-on: ubuntu-latest
    outputs:
      run_backend: ${{ steps.check_changes.outputs.RUN_BACKEND }}

    steps:
      - uses: actions/checkout@v4

      - name: Fetch default branch
        run: git fetch origin ${{ github.event.repository.default_branch }}:${{ github.event.repository.default_branch }}

      - name: Check for changes in the backend
        id: check_changes
        run: |
          diff=$(git diff --name-only ${{ github.event.repository.default_branch }} ${{ github.sha }} -- backend/)
          echo $diff
          if [ -n "$diff" ];  # Check if `diff` is not empty
          then
            result="true"
          else
            result="false"
          fi
          echo $result
          echo "RUN_BACKEND=$result" >> $GITHUB_OUTPUT
        shell: bash

  build-backend:
    needs: check_changes
    if: ${{ needs.check_changes.outputs.run_backend == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: danya151q/feedback:backend_pull_request

  linter-backend:
    needs: [ build-backend ]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Pull image
        run: docker pull danya151q/feedback:backend_pull_request

      - name: Run linters
        run: docker run danya151q/feedback:backend_pull_request linters